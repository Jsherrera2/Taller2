@model IEnumerable<BEUEjercicio.Pelicula>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre_peli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_lanzamiento_peli)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.categoria_pel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.duracion_peli)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre_peli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_lanzamiento_peli)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categoria_pel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.duracion_peli)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_pel }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_pel }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_pel })
        </td>
    </tr>
}

</table>
